/**
 * /h/{hubId}/home — Hub dashboard.
 * Re-exports the main catalyst dashboard page which renders
 * the correct hub-specific view based on user context.
 */
export { default } from '@/app/(authenticated)/catalyst/page'
