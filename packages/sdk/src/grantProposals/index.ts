/**
 * Spec 003 — Intent Marketplace (Proposal Lane). Grant proposals barrel (T043).
 */

export { GrantProposalClient } from './client'
export type {
  IGrantProposalClient,
  McpInvoker,
  McpTarget,
} from './client'
export type {
  GrantProposal,
  GrantProposalStatus,
  Budget,
  BudgetLineItem,
  Milestone,
  DesiredOutcome,
  ReportingObligations,
  OrganisationalBackground,
  SubmitGrantProposalRequest,
  EditGrantProposalRequest,
  SubmitGrantProposalError,
  SubmitGrantProposalResult,
  WithdrawGrantProposalResult,
} from './types'
